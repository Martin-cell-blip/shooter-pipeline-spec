# 测试夹具：2026-09-08 补丁的「改动前」快照

**这不是历史抓取，是构造的夹具。** 构造方法：取 `config/baseline/` 的当前值（已含 09-08 改动后的数值），对 `config/patches/2026-09-08.yaml` 中列出的每个字段回填官方给出的 `from` 值，其余字段原样保留。

有效前提（任一不满足则本夹具不可用于回放）：
1. 官方补丁条目覆盖了该英雄在 09-08 的**全部**数值改动（范围完整）；
2. 09-08 之后到基线抓取日（09-22）之间，这些字段没有被再次改动（无后续覆盖）——官方页面同期的 09-10、09-17 两期未见雾子与温斯顿条目，据此判定成立。

构造脚本：`python -m pipeline.fixture build 2026-09-08`（可重跑，结果幂等）。`provenance.fixture` 段记录了每个被回填的字段。
